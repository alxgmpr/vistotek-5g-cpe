# OpenWrt build checkout — regenerated by openwrt-port/docker/build.sh
/openwrt/

# Large binary recon dumps / recovery backups (kept locally, not in git)
*.bin

# Extracted stock firmware (recon, large)
/rootfs/
/ubi_images/
/ubi_extract/
/overlay_extract/

# Python venv
/.venv-ubi/

# Build outputs and logs
openwrt-port/artifacts/
openwrt-port/build.log
openwrt-port/*.err

# Staged TFTP images (large, regenerated)
openwrt-port/tftp/*.itb

# Recon logs / derived output
/boot.log
/screenlog.0
/wtinfo_decrypted.json

# macOS
.DS_Store

# incremental-build overlay tracking (build state)
openwrt-port/.overlay-manifest
